<%= form_with(model: aquery, local: true) do |form| %> <% if aquery.errors.any?
%>
<div id="error_explanation">
  <h2>
    <%= pluralize(aquery.errors.count, "error") %> prohibited this aquery from
    being saved:
  </h2>

  <ul>
    <% aquery.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="col-12 col-md-9 mb-2 mb-md-0 ml+7">
  <% form.label :query_value %>
  <%= form.text_field :query_value %>
</div>
<br>
<div class="col-12 col-md-3">
  <%= form.submit "Submit", class: "btn btn-primary", data: {disable_with: "Processing...".html_safe} %>
</div>


<% end %>